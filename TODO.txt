1. проверка работы http2 внутри proxy, через чекбокс (для http, scks4 это по умолчанию не доспупно?)

curl -L -I --http2  -x socks5://5.75.202.83:10000 https://chat.openai.com

2. проблема с темой, не берётся системная
3. два языка: русский и английский, переключение между языками
4. тесты
5. раздел About с нашей иконкой
6. иконка на панели запущенных приложений
7. whois страны для тех источников, где её нет
8. запуск и останов по кнопке некоей горутины для мониторинга доступности раз в минуту выбранного прокси
9. Добавить источник https://github.com/proxifly/free-proxy-list/tree/main/proxies/protocols/socks5
10. Добавить источник https://proxyverity.com/free-proxy-list/
11. linters

в checkProxy есть такой код

	if target == "" {
		target = "https://google.com"
	}

мне он не нравится, т.к. в вслучае выбора проверки прокси с типом http и остутвуимем целевого сайта -  мы будем првоерить https транспорт, хотя http прокси не поддерживает его

есть и обратная проблема, при замене  тут в коде схемы на Http - тогда нельзя будет честно проверить доступность http2

 стоит указыаать целевые сайты бех схемы и хранить их в конфиге без схемы, а схему выбирать исходя из типа прокси:
-  http proxy -> http schema
-  https proxy -> https schema
-  socks4 proxy -> http schema
-  socks5 proxy -> http schema

если меппинг верный, то давай реаоизуем